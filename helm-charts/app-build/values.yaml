# Default values for build.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: gitops-demo-system
  tag: latest

metadata:
  name: gitops-demo-system
  namespace: test

repository:
  uri: https://github.com/ozyohthree/gitops-demo-system.git
  branch: 
  directory: /
